# MobileProgramming

This repo contains code a android app which has two activity, lets say if those two acitivity are first activity and second activity. From first acitivity it needs to pass message and listen from the second activity also second activity needs to pass the message back to the first acitivity.
